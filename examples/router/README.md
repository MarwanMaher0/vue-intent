# Router Example

Demonstrates `useIntentNavigation` with Vue Router.

## Run

From the repo root:

```bash
npm install
vite --config vite.example.router.config.ts
```

Then open the printed local URL.
